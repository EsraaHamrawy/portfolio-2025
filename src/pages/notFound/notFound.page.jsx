import styles from "./notFound.module.css";
const NotFound = () => {
  return (
    <div
      style={{
        height: "100vh",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      Page not found
    </div>
  );
};

export default NotFound;
